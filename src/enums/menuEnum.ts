/**
 * @description: menu type
 */
export enum MenuTypeEnum {
  // left menu
  SIDEBAR = 'sidebar',
  MIX = 'mix',
  // top menu
  TOP_MENU = 'top-menu',
}

// menu mode
export enum MenuModeEnum {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
  MIX = 'mix',
}
