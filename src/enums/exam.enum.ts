export default {
  /**
   * @param { status } 考试状态
   * @param { status } \[0]未开始
   * @param { status } \[1]考试中
   * @param { status } \[2]考试结束
   */
  status: ['NOT_STARTED', 'EXAMING', 'FINISHED'],
}
